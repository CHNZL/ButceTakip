sed -i 's/onValueChange = { newValue -> if (newValue.all { it.isDigit() || it == '\''.'\'' || it == '\'','\'' }) amountText = newValue }/onValueChange = { amountText = com.example.util.formatInputAmount(it) }/g' app/src/main/java/com/example/ui/AddTransactionDialog.kt
sed -i 's/onValueChange = { newValue -> if (newValue.all { it.isDigit() || it == '\''.'\'' || it == '\'','\'' }) quantityText = newValue }/onValueChange = { quantityText = com.example.util.formatInputAmount(it) }/g' app/src/main/java/com/example/ui/AddTransactionDialog.kt
sed -i 's/onValueChange = { newValue -> if (newValue.all { it.isDigit() || it == '\''.'\'' || it == '\'','\'' }) unitPriceText = newValue }/onValueChange = { unitPriceText = com.example.util.formatInputAmount(it) }/g' app/src/main/java/com/example/ui/AddTransactionDialog.kt
